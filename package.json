{
  "name": "semsql-monorepo",
  "private": true,
  "version": "0.1.0-alpha.2",
  "description": "SemanticSQL workspace \u2014 TypeScript extractors and CLI",
  "license": "Apache-2.0 OR MIT",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "tsx": "^4.19.1",
    "vitest": "^2.1.1",
    "@biomejs/biome": "^1.9.3"
  }
}
