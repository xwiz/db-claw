"""SemanticSQL Python namespace.

The benchmark runtime currently lives in the Rust CLI and eval packages; this
namespace stays intentionally small until the embeddable Python SDK surface is
ready to share the same runtime contract.
"""

__version__ = "0.1.0.dev0"
