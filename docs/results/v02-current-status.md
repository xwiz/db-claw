# v0.2 Current Status
2026-06-09: guarded-read private alpha; accepted wrong SQL `0`; four-way typed decision spine is live.
Proof: duplicate `enterprise` values returned `ask_user`; explicit drift-keyed approval reran as `execute` on `packages.package_tier`. A ranking mismatch returned `ask_llm`; model SQL remains non-executable.
BIRD first20: `7/20`, wrong `0`, bailed `13`; raw-DB stress only. See [ledger](v02-evidence-ledger.md) and [plan](v02-semantic-atlas-completion-plan.md).
Next: broader contracts, Laravel end-to-end, resolver visual QA, and release rehearsal.
